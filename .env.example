# FinLogic Portal — environment variables for network/server setup
# Copy to .env.local (or set in your process manager / container).

# Required: Supabase API URL (use your local Supabase URL when DB is migrated)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Required: Supabase anon/public key (use your local Supabase anon key when DB is migrated)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
